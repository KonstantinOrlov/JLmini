<UserControl x:Class="JLmini.View.QuestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <!--<UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
        </Style>
    </UserControl.Resources>-->
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ListBox Grid.Row="0" ItemsSource="{Binding Questions}"
                 SelectedItem="{Binding SelectedQuestion}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Margin="5" Text="{Binding Path=QuestionText}" />
                        <TextBlock Margin="5" Text="{Binding Path=Section}" />
                        <TextBlock Margin="5" Text="{Binding Path=Stage}" />
                        <TextBlock Margin="5" Text="{Binding Path=Points}" />
                        <TextBlock Margin="5" Text="{Binding Path=Time}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Width="80" Command="{Binding AddQuestion}">Add</Button>
            <Button Width="80" Command="{Binding RemoveQuestion}" CommandParameter="{Binding SelectedQuestion}">Remove</Button>
        </StackPanel>
    </Grid>
</UserControl>
